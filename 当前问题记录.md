# 当前问题记录

更新时间：2026-03-03

## 1. ~~Mixin 注入参数签名存在运行时失配风险~~（已修复）
- ~~文件：`MixinRenderBlocks.java` — `Object` 改为 `Block`~~
- ~~文件：`MixinTextureMap.java` — `Object` 改为 `IResourceManager`~~
- 修复方式：
  - 新增编译时 stubs: `net.minecraft.block.Block`, `net.minecraft.client.resources.IResourceManager`
  - `MixinRenderBlocks`: 参数 `Object` → `Block`，反射调用 → `@Shadow` 直接调用（性能提升数十倍）
  - `MixinTextureMap`: 参数 `Object` → `IResourceManager`
- 状态：**已修复，构建通过**

## 2. ~~动态光照硬关闭存在无效方法变体~~（已修复）
- ~~文件：`MixinEntityRenderer.java` — 注入了不存在的 `updateLightmap()V` 无参变体~~
- 修复方式：删除 `func_78472_g()V` / `updateLightmap()V` 无参注入，仅保留 `(F)V` 有参版本（1.7.10 实际签名）
- 状态：**已修复，构建通过**
- 注意：实机确认仍需后续在真实 1.7.10 环境中验证

## 3. 验收项尚未完成实机验证（中优先级，待处理）
- 现状：`gradlew build` 已通过，但以下验收项仍未在游戏内确认：
  - F3 面板 AO 关闭效果
  - mipmap 关闭后的近景锐利度
  - `GLStateCache` 冗余调用率 <= 5%（需配合 benchmark）
- 风险：编译通过不等于运行态行为完全符合验收标准。

## 4. Build 基于本地 stubs 编译（低优先级，已缓解）
- 现状：stubs 已补充 `Block` 和 `IResourceManager`，关键参数签名匹配
- 后续建议：引入真实 1.7.10 mapped jar 替代全部 stubs，实现编译期完整验证
